make bench-bolt: target not found in Makefile

Phase 4 baseline carry-forward note (mirrors Phase 1 posture):
The `bench-bolt` Makefile target was scoped for Phase 12 (Performance Gates &
Hardening). Phase 1's `bench-bolt-phase1.txt` records the same "target not
found" result. Phase 12 owns the formal regression gate (CLAUDE.md: `make
bench-bolt` Δ ≤ 5% measured against pre-M1 baseline).

What Phase 4 DOES capture (in `bench-observe-hot-phase4.txt`):
- `BenchmarkObserve_HotBolt/*` sub-benches: connections / messages / decode
  errors all 0 allocs/op — well under the ≤ 2 allocs/op budget (MET-25).

Phase 12 will:
1. Add `bench-bolt` Makefile target (or document the canonical invocation).
2. Run regression vs `bench-bolt-phase1.txt` baseline.
3. Sign off the Δ ≤ 5% perf gate in ADR §4.1 row 12.
