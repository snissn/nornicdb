make bench-cypher: target not found in Makefile

Phase 4 baseline carry-forward note (mirrors Phase 1 posture):
The `bench-cypher` Makefile target was scoped for Phase 12 (Performance Gates &
Hardening). Phase 1's `bench-cypher-phase1.txt` records the same "target not
found" result. Phase 12 owns the formal regression gate (CLAUDE.md: `make
bench-cypher` Δ ≤ 2%, `make bench-bolt` Δ ≤ 5% measured against pre-M1
baseline).

What Phase 4 DOES capture (in `bench-observe-hot-phase4.txt`):
- Per-subsystem `BenchmarkObserve_Hot/<subsystem>` micro-benches.
- Every Hot sub-bench (except the documented `hot_with_span` Phase-6 forward
  simulation) lands at 0 allocs/op — well under the ≤ 2 allocs/op budget
  (CONTEXT D-02b / MET-25).

Cumulative inputs from per-plan bench files:
- `.planning/phases/04-subsystem-metric-catalog/bench-http-bolt-04-02.txt`
- `.planning/phases/04-subsystem-metric-catalog/bench-cypher-04-03.txt`
- `.planning/phases/04-subsystem-metric-catalog/bench-storage-mvcc-04-04.txt`
- `.planning/phases/04-subsystem-metric-catalog/bench-embed-search-04-05.txt`
- `.planning/phases/04-subsystem-metric-catalog/bench-replication-auth-04-06.txt`

Phase 12 will:
1. Add `bench-cypher` / `bench-bolt` Makefile targets (or document they remain
   `go test -bench` invocations under specific build tags).
2. Run the regression gate against `bench-cypher-phase1.txt` baseline.
3. Sign off the Δ ≤ 2% / Δ ≤ 5% perf gates in §4.1 row 12.
